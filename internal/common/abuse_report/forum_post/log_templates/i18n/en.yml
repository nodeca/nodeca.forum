en-US:
  subject:
    subject: "Report from #{user} (forum)"
  body:
    header: "Report from user #{url}:"
    to: "About [forum post](#{url}):"
    message_sent_to: "This report was also sent to: "
    move_to:
      User suggests to move topic
       "[#{src_title}](#{src_url})"
       from "[#{move_from_title}](#{move_from_link})"
       into "[#{move_to_title}](#{move_to_link})".
    move_link: "Click this link if you think it is correct:"
