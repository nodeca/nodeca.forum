en-US:
  subject:
    subject: "report from #{user} (forum)"
  body:
    header: "Report from user #{url}:"
    to: "About forum post:"
    message_sent_to: "This report was also sent to: "
    report_topic: Topic with report.
    move_to:
      User suggests to move topic
       "<a href="#{src_title}">#{src_url}</a>"
       from "<a href="#{move_from_link}">#{move_from_title}</a>"
       into "<a href="#{move_to_link}">#{move_to_title}</a>".
    move_link: "Click this link if you think it is correct:"
