router:
  map:
    forum.section.list:
      "/": ~

    forum.thread.list:
      "/f{id}":
        id: /\d+/

    forum.thread.show:
      "/f{forum_id}/thread{id}":
        forum_id: /\d+/
        id: /\d+/
