setting_groups:
  forum: ~

  forum_general:
    parent: forum


setting_schemas:
  global:
    topics_per_page:
      type: number
      group_key: forum_general
      category_key: forum
      default: 30

    posts_per_page:
      type: number
      group_key: forum_general
      category_key: topic
      default: 25
