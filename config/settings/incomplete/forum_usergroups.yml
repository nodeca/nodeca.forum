setting_schemas:
  forum_usergroup:
    forum_show:
      extends: true
      priority: 10

    forum_read_topics:
      extends: true
      priority: 20

    forum_reply_topics:
      extends: true
      priority: 30

    forum_start_topics:
      extends: true
      priority: 40
