if self.post_idx > 0
  if !self.ignored_users[self.posts[self.post_idx].user] && !self.ignored_users[self.posts[self.post_idx - 1].user] || self.settings.forum_show_ignored
    - var t1 = new Date(self.posts[self.post_idx - 1].ts)
    - var t2 = new Date(self.posts[self.post_idx].ts)
    if Math.abs(t2 - t1) > self.settings.forum_show_post_interval * 24*60*60*1000 && self.settings.forum_show_post_interval > 0
      footer.forum-postlist__datediff.forum-post__before
        span.icon.icon-clock.icon-space-after=self.t('date_interval', { interval: self.date(new Date(Date.now() - t2.getTime() + t1.getTime()), 'relative_days') })
